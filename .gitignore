# Personal usage data generated by the parsers — never commit these
usage_data.json
copilot_data.json
usage.db

# Local Claude Code settings (machine-specific, may contain local paths)
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
*.bat
__pycache__/
*.pyc
.venv/
venv/
